# Blue Shard

A Graphics Library. Still a Work in Progress.

## Requirements

- pybind11

## Build

```Bash
cmake -B build
cmake --build build
```

## Run

```Bash
./run examples/spinning_boxes.py
```